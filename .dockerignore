# Docker build context of the sync server: exclude artifacts and caches, keep sources + wrapper.
.git
.gradle
.kotlin
.idea
**/build
!server/build/install/server
*.db
*.db-journal
local.properties
docs
